# show-users-permission-in-linux
the script extracts information about users from the /etc/passwd file, determines if they are root or regular users, and outputs the information in a tab-delimited format.
